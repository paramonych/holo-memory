function shouldersFrom(left, right) {
    return {
        left: left,
        right: right
    };
}
function shoulderFrom(mesh, light) {
    return {
        mesh: mesh,
        light: light
    };
}
